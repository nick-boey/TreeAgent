# Homespun Environment Configuration
# Copy this file to .env and fill in your values
#
# Recommended: Place credentials in ~/.homespun/env instead:
#   export GITHUB_TOKEN=ghp_...
#   export CLAUDE_CODE_OAUTH_TOKEN=...
#   export TAILSCALE_AUTH_KEY=tskey-auth-...
#
# For Azure VM deployments, you can set these as VM secrets:
#   HSP_GITHUB_TOKEN              - GitHub token
#   HSP_CLAUDE_CODE_OAUTH_TOKEN   - Claude Code OAuth token
#   HSP_TAILSCALE_AUTH_KEY        - Tailscale auth key

# Required: GitHub personal access token for PR synchronization
# Create at: https://github.com/settings/tokens (needs 'repo' scope)
# The script checks HSP_GITHUB_TOKEN first, then GITHUB_TOKEN
GITHUB_TOKEN=ghp_your_token_here

# Required: Claude Code OAuth token for API authentication
# Run 'claude login' on the host machine to authenticate, then
# copy the token from ~/.claude/.credentials.json
# The script checks HSP_CLAUDE_CODE_OAUTH_TOKEN first, then CLAUDE_CODE_OAUTH_TOKEN
CLAUDE_CODE_OAUTH_TOKEN=

# Optional: Tailscale auth key for VPN access
# Generate at: https://login.tailscale.com/admin/settings/keys
# The script checks HSP_TAILSCALE_AUTH_KEY first, then TAILSCALE_AUTH_KEY
# TAILSCALE_AUTH_KEY=tskey-auth-your_key_here

# Optional: External hostname for agent URLs
# This makes agent links point to an external hostname instead of localhost
# HSP_EXTERNAL_HOSTNAME=homespun.example.com

# Optional: Watchtower polling interval in seconds (default: 300 = 5 minutes)
# WATCHTOWER_POLL_INTERVAL=300
