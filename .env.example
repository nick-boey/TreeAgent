# Homespun Environment Configuration
# Copy this file to .env and fill in your values
#
# For Azure VM deployments, you can set these as VM secrets instead:
#   HSP_GITHUB_TOKEN     - GitHub token
#   HSP_TAILSCALE_AUTH   - Tailscale auth key

# Required: GitHub personal access token for PR synchronization
# Create at: https://github.com/settings/tokens (needs 'repo' scope)
# The script checks HSP_GITHUB_TOKEN first, then GITHUB_TOKEN
GITHUB_TOKEN=ghp_your_token_here

# Optional: Tailscale configuration for secure remote access
# Generate auth key at: https://login.tailscale.com/admin/settings/keys
# The script checks HSP_TAILSCALE_AUTH first, then TAILSCALE_AUTH_KEY
# TAILSCALE_AUTH_KEY=tskey-auth-your_key_here
# TAILSCALE_HOSTNAME=homespun-vm

# Optional: External hostname for agent URLs (when running in Tailscale mode)
# This makes agent links point to the Tailscale hostname instead of localhost
# Format: hostname.tailnet.ts.net (get your tailnet name from Tailscale admin console)
# HSP_EXTERNAL_HOSTNAME=homespun.tail1234.ts.net

# Optional: Watchtower polling interval in seconds (default: 300 = 5 minutes)
# WATCHTOWER_POLL_INTERVAL=300
